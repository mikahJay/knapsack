# infra

Terraform skeleton and notes for deploying the services to AWS (test environment).

This folder contains a minimal Terraform layout. It's a starting point — expand with modules, state backend, and CI integration.

Local environment: use the `local` mode which runs services in-memory (no AWS required).
